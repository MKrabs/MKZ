// LoginNudge component removed — kept placeholder file to avoid accidental imports.
// The animated login nudge (arrow + floating bubble) has been deleted.
// If you need to reintroduce a simpler sign-in prompt, update PlateSubmission to render it.

export default null as any;
